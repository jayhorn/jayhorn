//install the commit hook if possible.
def hook = new File('pre-commit.sh')
def hook_folder = new File('./.git/hooks')
def installed_hook = new File('./.git/hooks/pre-commit')

if (hook.exists() && hook_folder.exists() && !installed_hook.exists()) {
  println("Installing pre-commit hook")
// DD: This does not work on windows and I dont know how to ignore the error
// But it should be on the top level  
//  exec {
//      workingDir '.'
//	  commandLine 'copy', hook, installed_hook
//  }  
}

subprojects {
    apply plugin: 'java'
}

project(':jayhorn') {
    dependencies {
        compile project(':soottocfg')
    }
}

project(':soottocfg') {
 
}